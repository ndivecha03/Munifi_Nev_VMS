{
  "schemaVersion": 1,
  "pack": "gov-dispatch",
  "name": "Gov Dispatch",
  "vertical": "Gov",
  "repo": "https://github.com/ndivecha03/MunifiGov_Dispatch_Pack",
  "buyer": "DPW Director / SR Coordinator",
  "price": "$50K/yr",
  "status": "Sellable demo",
  "overall": 60,
  "lastReviewed": "2026-06-15",
  "desc": "Dispatcher pack - 6 agents over the SF vendor matrix. Runs end-to-end in-browser on real SF data; remaining work is unattended (scheduled) operation and a durable Supabase audit log.",
  "agents": [
    { "id": "compliance-memo", "name": "compliance-memo", "pct": 85, "status": "live",
      "entrypoint": "api/generate-14b-memo.py",
      "dataSources": ["request payload"],
      "openItems": ["verify CMD cert before issuing", "archive generated memos", "auto-fill signer + e-signature"],
      "nextSteps": ["Archive every memo to a memo_records table (hash, vendor_id, complaint_id, signer)", "Verify cert status pre-generation", "Auto-fill signer from session; add e-signature plumbing"] },
    { "id": "vendor-cert-watchdog", "name": "vendor-cert-watchdog", "pct": 80, "status": "live",
      "entrypoint": "api/v2/watchdog.js",
      "dataSources": ["Supabase: cert_watchdog_60d view", "Supabase: certifications"],
      "openItems": ["add a schedule (currently manual trigger)", "thresholds hardcoded", "no fallback if Supabase is down"],
      "nextSteps": ["Wrap in a daily Vercel Cron + email the digest", "Add an ACTION ITEMS section (who to email to renew)", "Emit cert.expired so compliance agents exclude lapsed vendors"] },
    { "id": "equity-dispatch", "name": "equity-dispatch", "pct": 75, "status": "partial",
      "entrypoint": "agents/equity_dispatch/agent.py",
      "dataSources": ["static: baked-vendors-v2.json", "Supabase: /api/v2/vendors (browser)", "static: complaints.json"],
      "openItems": ["production CLI path not yet wired to Supabase", "browser-only (no headless mode)", "scoring inputs static"],
      "nextSteps": ["Implement the Supabase fetch+rank path in the Python agent", "Wire reliability to feedback.outcome_recorded events", "Add --output json for agent-to-agent calls"] },
    { "id": "vehicle-router", "name": "vehicle-router", "pct": 70, "status": "partial",
      "entrypoint": "agents/vehicle_router/agent.py",
      "dataSources": ["hardcoded NAICS pool map"],
      "openItems": ["pool size hardcoded (no live count)", "no district granularity", "no production mode"],
      "nextSteps": ["Replace the mock with a live eligible-vendor COUNT query", "Add --district for location-aware routing", "Emit dispatch.set_aside_insufficient_pool when pool < 3"] },
    { "id": "outcome-feedback", "name": "outcome-feedback", "pct": 55, "status": "partial",
      "entrypoint": "api/feedback.js",
      "dataSources": ["Resend", "audit_events"],
      "openItems": ["needs a citizen_email source (DataSF does not expose it)", "token validation is format-only", "no reliability feedback pipeline"],
      "nextSteps": ["Source citizen email at dispatch time", "Validate token against the original dispatch event (lookup, not format)", "Build the outcome->reliability pipeline + a 7-day schedule"] },
    { "id": "assignment-distribution", "name": "assignment-distribution", "pct": 45, "status": "weak",
      "entrypoint": "scripts/agents/assignment-agent.py",
      "dataSources": ["static: complaints.json", "static: staff.json", "static: assignments.json"],
      "openItems": ["CLI-only (no schedule)", "no load rebalancing", "emits no audit event"],
      "nextSteps": ["Port to a 6h Vercel Cron reading Supabase", "Emit assignment.generated / assignment.reassigned", "Add --rebalance to rehash orphaned complaints"] }
  ]
}
